# Test Runner Configuration for MCP Hub

# Test categories
# Run only unit tests:
# dotnet test --filter Category=Unit

# Run only integration tests:
# dotnet test --filter Category=Integration

# Run only end-to-end tests:
# dotnet test --filter Category=EndToEnd

# Run all tests except long-running:
# dotnet test --filter Category!=LongRunning

# Run tests with coverage:
# dotnet test --collect:"XPlat Code Coverage" --settings coverlet.runsettings

# Run tests with coverage and threshold:
# dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura /p:Threshold=90

# Common test commands:
# dotnet test                           # Run all tests
# dotnet test --verbosity normal        # Run with normal verbosity
# dotnet test --logger trx             # Output test results in TRX format
# dotnet test --logger html            # Output test results in HTML format
# dotnet test --parallel               # Run tests in parallel
# dotnet test --no-build               # Run tests without building
# dotnet test --blame                  # Enable blame mode for crash detection